route:
  receiver: omegalb-webhook
  group_by: ['alertname', 'service']
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 2h

receivers:
  - name: omegalb-webhook
    webhook_configs:
      - url: http://alert-sink:8088/webhook
        send_resolved: true
